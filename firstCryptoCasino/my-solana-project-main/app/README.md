# Last Move Game - Frontend

Frontend приложение для игры "Last Move Game" на Solana.

## 🚀 Быстрый старт

### Установка зависимостей:

```bash
npm install
# или
yarn install
```

### Настройка окружения:

Создайте файл `.env.local` на основе `.env.local.example`:

```bash
cp .env.local.example .env.local
```

### Запуск dev сервера:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
app/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Root layout с WalletProvider
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── GameStatus.tsx    # Отображение состояния игры
│   ├── GameActions.tsx   # Кнопки действий (make_move, claim_win)
│   └── WalletProvider.tsx # Провайдер для Solana wallet
├── hooks/                 # Custom React hooks
│   ├── useGame.ts        # Hook для работы с игрой
│   ├── useTimer.ts       # Hook для таймера
│   └── useEvents.ts      # Hook для подписки на события
├── lib/                   # Утилиты
│   └── anchor.ts         # Anchor client setup
└── types/                 # TypeScript типы
    └── my_solana_project.ts
```

## 🔧 Основные функции

### useGame Hook
- Загрузка состояния игры
- Real-time обновления через подписку
- Функции `makeMove()` и `claimWin()`

### useTimer Hook
- Расчет оставшегося времени таймера
- Автоматическое обновление каждую секунду
- Форматирование времени (MM:SS)

### useEvents Hook
- Подписка на события контракта
- Отслеживание MoveMade, GameEnded, TimerReduced

## 🎨 Компоненты

### GameStatus
Отображает:
- Статус игры (Active/Paused/Inactive)
- Текущий раунд
- Размер джекпота
- Стоимость хода
- Таймер обратного отсчета
- Последний игрок

### GameActions
Кнопки для:
- Make Move - сделать ход
- Claim Win - забрать выигрыш (только для победителя)

## 🔌 Интеграция с контрактом

Frontend использует:
- **Anchor client** для вызова функций контракта
- **Solana wallet adapter** для подключения кошельков
- **Web3.js** для подписки на события и аккаунты

### Подключение кошелька:
1. Нажмите "Select Wallet"
2. Выберите Phantom или другой поддерживаемый кошелек
3. Подтвердите подключение

### Сделать ход:
1. Убедитесь что кошелек подключен
2. Убедитесь что игра активна
3. Нажмите "Make Move"
4. Подтвердите транзакцию в кошельке

## 📝 Примечания

- Для работы требуется подключенный Solana кошелек
- Транзакции требуют подтверждения в кошельке
- Состояние игры обновляется в реальном времени
- Таймер обновляется каждую секунду

## 🛠️ Разработка

```bash
# Dev режим
npm run dev

# Сборка
npm run build

# Production запуск
npm start

# Линтинг
npm run lint
```

## 📚 Дополнительная информация

- [Next.js Documentation](https://nextjs.org/docs)
- [Solana Wallet Adapter](https://github.com/solana-labs/wallet-adapter)
- [Anchor Documentation](https://www.anchor-lang.com/)
